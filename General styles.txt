/* General styles */
body {
  margin: 0;
  font-family: 'Arial', sans-serif;
  background-color: #1a001a; /* dark purple/black */
  color: #fff;
}

/* Header */
header {
  background: linear-gradient(90deg, #660066, #330033); /* purple gradient */
  text-align: center;
  padding: 2rem;
}

header h1 {
  margin: 0;
  color: #ff99ff;
}

header p {
  margin: 0.5rem 0;
}

nav a {
  color: #ffccff;
  margin: 0 1rem;
  text-decoration: none;
  font-weight: bold;
}

/* Sections */
section {
  padding: 2rem;
  text-align: center;
}

.clips-container {
  display: flex;
  justify-content: center;
  gap: 1rem;
  flex-wrap: wrap;
}

.clip {
  background-color: #330033;
  padding: 1rem;
  border-radius: 10px;
  width: 250px;
}

.clip img {
  max-width: 100%;
  border-radius: 10px;
}

/* Buttons */
.btn {
  display: inline-block;
  padding: 0.5rem 1rem;
  margin: 0.5rem;
  background-color: #990099;
  color: #fff;
  text-decoration: none;
  border-radius: 5px;
  font-weight: bold;
  transition: 0.3s;
}

.btn:hover {
  background-color: #cc00cc;
}

/* Footer */
footer {
  background-color: #660066;
  padding: 1rem;
  text-align: center;
  margin-top: 2rem;
}
